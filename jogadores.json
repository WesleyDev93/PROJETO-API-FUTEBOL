{
  "jogadores": [
    {
      "Jogador": "Exemplo",
      "gols": "5",
      "id": 1
    }
  ]
}

formulario.addEventListener("submit", (evento) => {
   
  const Jogador = evento.target.querySelector(".Jogador").value
  const gols = evento.target.querySelector(".gols").value

})
